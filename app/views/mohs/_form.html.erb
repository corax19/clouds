<%= form_with(model: moh) do |form| %>
  <% if moh.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(moh.errors.count, "error") %> prohibited this moh from being saved:</h2>

      <ul>
        <% moh.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label I18n.t(:name), style: "display: block" %>
    <%= form.text_field :name , {class: "col-2 form-control rounded-pill"} %>
  </div>

  <div>
    <%= form.label I18n.t(:description), style: "display: block" %>
    <%= form.text_field :description , {class: "col-2 form-control rounded-pill"} %>
  </div>

  <div>
    <br><%= form.submit btitle, :class => 'btn btn-sm btn-success btn-pill' %>
  </div>
<% end %>

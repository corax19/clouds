<h1>Edit Voicemail step</h1>

<%= render "formvoicemail", step: @step , locals: {:btitle => "Update Voicemail Step", :formmethod => :put, :url => update_stepvoicemail_path(@step.route_id,@step.id)} %>

<br>

<div>
  <%= link_to "Back to steps", steps_path %>
</div>
